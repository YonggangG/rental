import { AdminShell } from '@/components/admin/AdminShell';import { Field,inputClass } from '@/components/admin/Field';import { getLocale,statusLabel,t } from '@/lib/i18n';import { prisma } from '@/lib/prisma';import { requireAdmin } from '@/lib/auth/session';import { createCharge,deleteCharge } from '../actions';
const iso=(d:Date)=>d.toISOString().slice(0,10);
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){await requireAdmin();const sp=await searchParams,locale=getLocale(sp),m=t(locale);const [rows,leases]=await Promise.all([prisma.rentCharge.findMany({orderBy:{dueDate:'desc'},include:{lease:{include:{property:true,tenants:{include:{tenant:true}}}},payments:true}}),prisma.lease.findMany({include:{property:true,tenants:{include:{tenant:true}}}})]);return <AdminShell locale={locale} title={m.rentLedger}><section className="card"><h2 className="text-xl font-semibold">{m.createCharge}</h2><form action={createCharge} className="mt-4 grid gap-4 md:grid-cols-5"><input type="hidden" name="lang" value={locale}/><Field label={m.leases}><select className={inputClass} name="leaseId" required>{leases.map(l=><option value={l.id} key={l.id}>{l.property.nickname||l.property.address1} - {l.tenants.map(x=>x.tenant.firstName).join(', ')}</option>)}</select></Field><Field label={m.dueDate}><input className={inputClass} name="dueDate" type="date" required/></Field><Field label={m.description}><input className={inputClass} name="description" defaultValue="Monthly rent"/></Field><Field label={m.amount}><input className={inputClass} name="amount" type="number" step="0.01" required/></Field><Field label={m.status}><select className={inputClass} name="status"><option value="OPEN">{statusLabel(locale,'OPEN')}</option><option value="PAID">{statusLabel(locale,'PAID')}</option><option value="LATE">{statusLabel(locale,'LATE')}</option><option value="PARTIAL">{statusLabel(locale,'PARTIAL')}</option></select></Field><Field label={m.lateFee}><input className={inputClass} name="lateFee" type="number" step="0.01"/></Field><div className="md:col-span-5"><button className="btn">{m.create}</button></div></form></section><section className="card mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left text-slate-500"><th>{m.property}</th><th>{m.tenant}</th><th>{m.dueDate}</th><th>{m.description}</th><th>{m.amount}</th><th>{m.status}</th><th>{m.actions}</th></tr></thead><tbody>{rows.map(r=><tr className="border-t" key={r.id}><td className="py-3 font-medium">{r.lease.property.nickname||r.lease.property.address1}</td><td>{r.lease.tenants.map(x=>`${x.tenant.firstName} ${x.tenant.lastName}`).join(', ')}</td><td>{iso(r.dueDate)}</td><td>{r.description}</td><td>${(Number(r.amount)+Number(r.lateFee)).toFixed(2)}</td><td>{statusLabel(locale,r.status)}</td><td><form action={deleteCharge}><input type="hidden" name="id" value={r.id}/><input type="hidden" name="lang" value={locale}/><button className="text-red-600">{m.delete}</button></form></td></tr>)}</tbody></table>{rows.length?null:<p className="text-sm text-slate-500">{m.noRecords}</p>}</section></AdminShell>}
