import { AdminShell } from '@/components/admin/AdminShell';import { Field,inputClass } from '@/components/admin/Field';import { getLocale,t } from '@/lib/i18n';import { prisma } from '@/lib/prisma';import { requireAdmin } from '@/lib/auth/session';import { createDocument,deleteDocument,uploadAdminDocument } from '../actions';
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){await requireAdmin();const sp=await searchParams,locale=getLocale(sp),m=t(locale);const [rows,properties,tenants,leases]=await Promise.all([prisma.document.findMany({orderBy:{createdAt:'desc'},include:{property:true,tenant:true,lease:{include:{property:true}}}}),prisma.property.findMany(),prisma.tenant.findMany(),prisma.lease.findMany({include:{property:true}})]);return <AdminShell locale={locale} title={m.documents}><section className="card"><h2 className="text-xl font-semibold">{m.createDocument}</h2><form action={uploadAdminDocument} encType="multipart/form-data" className="mt-4 grid gap-4 md:grid-cols-4"><input type="hidden" name="lang" value={locale}/><Field label={m.title}><input className={inputClass} name="title" required/></Field><Field label={m.kind}><input className={inputClass} name="kind" required/></Field><Field label={m.path}><input className={inputClass} name="path" placeholder="Optional URL/path"/></Field><Field label="Upload"><input className={inputClass} name="file" type="file"/></Field><Field label={m.property}><select className={inputClass} name="propertyId"><option value="">-</option>{properties.map(p=><option value={p.id} key={p.id}>{p.nickname||p.address1}</option>)}</select></Field><Field label={m.tenant}><select className={inputClass} name="tenantId"><option value="">-</option>{tenants.map(tn=><option value={tn.id} key={tn.id}>{tn.firstName} {tn.lastName}</option>)}</select></Field><Field label={m.leases}><select className={inputClass} name="leaseId"><option value="">-</option>{leases.map(l=><option value={l.id} key={l.id}>{l.property.nickname||l.property.address1}</option>)}</select></Field><div className="md:col-span-4"><button className="btn">{m.create}</button></div></form></section><section className="card mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left text-slate-500"><th>{m.title}</th><th>{m.kind}</th><th>{m.path}</th><th>{m.property}</th><th>{m.tenant}</th><th>{m.actions}</th></tr></thead><tbody>{rows.map(r=><tr className="border-t" key={r.id}><td className="py-3 font-medium">{r.title}</td><td>{r.kind}</td><td><a href={r.path}>{r.path}</a></td><td>{r.property?.nickname||r.property?.address1||'-'}</td><td>{r.tenant?`${r.tenant.firstName} ${r.tenant.lastName}`:'-'}</td><td><form action={deleteDocument}><input type="hidden" name="id" value={r.id}/><input type="hidden" name="lang" value={locale}/><button className="text-red-600">{m.delete}</button></form></td></tr>)}</tbody></table>{rows.length?null:<p className="text-sm text-slate-500">{m.noRecords}</p>}</section></AdminShell>}
