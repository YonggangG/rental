import { AdminShell } from '@/components/admin/AdminShell';import { Field,inputClass } from '@/components/admin/Field';import { getLocale,t } from '@/lib/i18n';import { requireAdmin } from '@/lib/auth/session';import { prisma } from '@/lib/prisma';import { notFound } from 'next/navigation';import { updateTemplate } from '../../actions';
export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){await requireAdmin();const [{id},sp]=await Promise.all([params,searchParams]);const locale=getLocale(sp),m=t(locale);const row=await prisma.leaseTemplate.findUnique({where:{id}});if(!row)notFound();return <AdminShell locale={locale} title={row.name}><section className="card"><h2 className="text-xl font-semibold">{m.edit}</h2><form action={updateTemplate} className="mt-4 grid gap-4 md:grid-cols-4"><input type="hidden" name="id" value={row.id}/><input type="hidden" name="lang" value={locale}/><Field label="Name"><input className={inputClass} name="name" defaultValue={row.name} required/></Field><Field label={m.version}><input className={inputClass} name="version" defaultValue={row.version}/></Field><Field label={m.jurisdiction}><input className={inputClass} name="jurisdiction" defaultValue={row.jurisdiction}/></Field><label className="flex items-center gap-2 pt-7 text-sm"><input name="active" type="checkbox" defaultChecked={row.active}/>Active</label><div className="md:col-span-4"><Field label={m.templateBody}><textarea className={inputClass+" min-h-[32rem] font-mono"} name="bodyMarkdown" defaultValue={row.bodyMarkdown} required/></Field></div><div className="md:col-span-4"><button className="btn">{m.update}</button></div></form></section></AdminShell>}
