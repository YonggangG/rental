import { TenantShell } from '@/components/TenantShell';import { getLocale,statusLabel,t } from '@/lib/i18n';import { requireUser } from '@/lib/auth/session';import { prisma } from '@/lib/prisma';
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const user=await requireUser();const sp=await searchParams,locale=getLocale(sp),m=t(locale);const rows=user.tenant?await prisma.maintenanceRequest.findMany({where:{tenantId:user.tenant.id},include:{property:true},orderBy:{createdAt:'desc'}}):[];return <TenantShell locale={locale} title={m.maintenance}><section className="grid gap-4">{rows.map(r=><article className="card" key={r.id}><h2 className="text-lg font-semibold">{r.title}</h2><p className="text-sm text-slate-500">{r.property.nickname||r.property.address1} · {statusLabel(locale,r.status)} · {r.priority}</p><p className="mt-3 text-sm">{r.description}</p></article>)}{rows.length?null:<p className="card text-sm text-slate-500">{m.noRecords}</p>}</section></TenantShell>}
